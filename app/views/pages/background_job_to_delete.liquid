{% liquid
  assign my_data = null | hash_merge: hello: 'world'

  background job = 'background_job/scheduled', delay: 10, source_name: 'background job to delete', data: my_data

  echo 'background job scheduled'
%}
